# @package _global_
defaults:
  - override /train_type: tres_regression
  - override /model: encoder
  - override /data: single

exp_project: noise_sig_experiments
exp_name: c_tres_regression_hs128

max_tres: 100.0

data:
  path: /home/plotnikovgp/baikal/data/baikal_mc_merged_m50-na15-nue15_norm.h5
  val_subset_cut: 3

model:
  params:
    hidden_size: 128
    num_layers: 5
    dim_feedforward_size: 512
    n_heads: 1
    out_size: 1

training:
  lr: 1.0e-3
  batch_size: 128
  warmup_steps: 5000
  num_train_steps_per_validation: 100
  valid_main_metric: loss
